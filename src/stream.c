#include "stream.h"



void stream_open(stream_t* op, const char* name, int oflag, mode_t mode) {
	
	//A FAIRE
	
}

int stream_close(stream_t* op) {
		
	//A FAIRE
	
	return 0;
}

int stream_read(stream_t* op, char* buffer, size_t size) {
		
	//A FAIRE
	
	return 0;
}

int stream_write(stream_t* op, char* buffer, size_t size) {
	
	//A FAIRE
	
	return 0;
}
